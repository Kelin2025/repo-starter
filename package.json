{
  "name": "repo-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "bun run --filter \"./packages/*\" dev",
    "start": "bun run --filter \"./packages/backend\" start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sst:dev": "sst dev --stage development",
    "sst:deploy": "sst deploy --stage production",
    "migrate:prod": "bunx sst shell --stage production bun packages/backend/scripts/migrate-prod.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ]
}